# API URL - Use relative path for production (nginx proxies /api to backend)
# Development: /api/v1 (via nginx proxy on port 3000)
# Direct backend access (if not using Docker): http://localhost:8080/api/v1
REACT_APP_API_URL=/api/v1
