# =============================================================================
# DEVELOPMENT CONFIGURATION ONLY
# =============================================================================
# This file is for local development only.
# Production MUST use environment variables (DB_URL, DB_USER, DB_PASSWORD, etc.)
# See .env.example for required environment variables
# =============================================================================

db.url=jdbc:mysql://localhost:3306/couponsystem?serverTimezone=UTC
db.user=YOUR_DB_USERNAME_HERE
db.password=YOUR_DB_PASSWORD_HERE

# Admin credentials - FOR DEVELOPMENT ONLY
# Production: Set ADMIN_EMAIL and ADMIN_PASSWORD environment variables
admin.email=your_admin_email@example.com
admin.password=your_admin_password_here

# WARNING: These are development defaults with weak passwords
# NEVER use these values in production
