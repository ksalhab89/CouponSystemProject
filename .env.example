# =============================================================================
# ENVIRONMENT VARIABLES - Required for Docker Compose
# =============================================================================
# Copy this file to .env and fill in your values
# The .env file is gitignored and should NEVER be committed
# =============================================================================

# MySQL Root Password (for docker-compose only)
MYSQL_ROOT_PASSWORD=your_secure_root_password_min_16_chars

# Database Configuration
DB_URL=jdbc:mysql://mysql:3306/couponsystem?serverTimezone=UTC
DB_USER=appuser
DB_PASSWORD=your_secure_db_password_min_16_chars

# Admin Credentials
ADMIN_EMAIL=admin@yourcompany.com
ADMIN_PASSWORD=your_secure_admin_password

# =============================================================================
# SECURITY REQUIREMENTS:
# =============================================================================
# 1. Use strong passwords (minimum 16 characters)
# 2. Mix uppercase, lowercase, numbers, and special characters
# 3. Never reuse passwords across environments
# 4. Rotate passwords every 90 days
# 5. After implementing bcrypt, ADMIN_PASSWORD must be a bcrypt hash
#
# Example strong password: mK9#vL2$nP8@qR5&wX3!
# Example bcrypt hash: $2a$12$N0LS1Z3Q5nY6LQaVCf6JXO7K...
# =============================================================================

# =============================================================================
# NOTES:
# =============================================================================
# - No SSL configuration needed (Docker internal network is isolated)
# - DB_URL uses 'mysql' as hostname (Docker service name)
# - config.properties is only used for local development outside Docker
# =============================================================================
