# Database Configuration
DB_URL=jdbc:mysql://localhost:3306/couponSystem?useSSL=false&serverTimezone=UTC
DB_USER=projectUser
DB_PASSWORD=your_secure_password_here

# Admin Credentials
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=your_secure_admin_password_here

# IMPORTANT SECURITY NOTES:
# 1. Never commit the actual .env file to version control
# 2. Use strong passwords in production
# 3. Consider using a secrets management service (AWS Secrets Manager, HashiCorp Vault, etc.)
# 4. Passwords in config.properties are for development only - use environment variables in production
